const LocalStrategy = require("passport-loccal").Strategy
const bcrypt = require("bcrypt")

function initialize(passport, useEmail, userId){
    const authenticateUser = async (email, password, done)=>{
        return
    }
}